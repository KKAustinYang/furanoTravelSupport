// The Modellix API key is no longer used in the browser.
// It is injected on the server by the serverless proxy (api/[...path].js)
// and by the Vite dev proxy. Configure it as MODELLIX_KEY (no VITE_ prefix).
